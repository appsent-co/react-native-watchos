pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,mjs,cjs,ts,tsx}'
      run: npx eslint {staged_files}
    types:
      glob: '*.{js,ts,tsx,d.ts}'
      run: npx tsc

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
