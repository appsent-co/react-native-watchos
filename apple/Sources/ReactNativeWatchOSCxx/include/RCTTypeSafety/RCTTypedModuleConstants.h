// Stub of `<RCTTypeSafety/RCTTypedModuleConstants.h>` for watchOS. Codegen
// `#import`s this in every `Native<Foo>Spec.h`; the `typedConstants<>`
// helper it declares is only referenced by specs with `constantsToExport`
// + typed-struct constants. v1 doesn't support that, so an empty header
// is sufficient.

#pragma once
