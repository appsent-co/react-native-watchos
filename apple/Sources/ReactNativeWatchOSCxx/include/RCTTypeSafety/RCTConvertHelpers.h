// Stub of `<RCTTypeSafety/RCTConvertHelpers.h>` for watchOS. Codegen
// `#import`s this in every `Native<Foo>Spec.h`; the helpers it declares
// (`RCTConvertVecToArray`, `RCTConvertOptionalVecToArray`, etc.) are only
// referenced by specs that use array / typed-object args. v1 doesn't
// support those, so an empty header is sufficient.

#pragma once
