// Stub of `<React/RCTManagedPointer.h>` for watchOS. Codegen `#import`s
// this in every `Native<Foo>Spec.h`; the `RCTManagedPointer` ObjC class
// it declares wraps `std::shared_ptr<void>` so struct converters can
// return C++ structs across the ObjC boundary. v1 doesn't support
// struct args, so an empty header is sufficient.

#pragma once
