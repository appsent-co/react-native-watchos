# macOS
.DS_Store

# Node
node_modules/
npm-debug.log

# Expo
.expo/

# Turborepo
.turbo/

# Xcode
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.xcuserstate
project.xcworkspace/
**/.xcode.env.local

# Swift Package Manager
.build/
.swiftpm/

# Hermes / XCFramework build outputs. `build/xcframework/` is rebuilt fresh
# before publishing via `./scripts/build-xcframework.sh` and bundled into the
# npm tarball — never commit it.
build/

# Expo prebuild outputs — regenerate via `pnpm prebuild` from app.json + targets/.
example/ios/

# Ruby (CocoaPods / ruby-lsp cache)
.ruby-lsp/

# Docusaurus
docs/.docusaurus/
docs/build/
docs/.cache-loader/

# Claude
.claude/
